/* use crate::{SamError, sam::{BamData, bam_data::BDAction, record::BamRec}};


impl BamRec {
    pub(in crate::sam) fn swap_data(&mut self, bd: &mut BamData, action: BDAction) -> Result<(), SamError> {
        
        
        
        Ok(())
    }
}
*/